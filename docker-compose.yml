version: '3.8'
services:
  darts-scorer:
    build: .
    ports:
      - "5054:5054"
    volumes:
      - .:/app
    environment:
      # Use a fixed secret key for all workers in the container
      - SECRET_KEY=a-very-secure-and-static-secret-key-for-darts