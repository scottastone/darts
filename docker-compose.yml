services:
  darts-scorer:
    build: .
    ports:
      - "5054:5054"
    volumes:
      - .:/app
    environment:
      # Use a fixed secret key for all workers in the container
      - SECRET_KEY=pick-a-key-here-or-use-this-one